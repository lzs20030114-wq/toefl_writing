# 真题丢题：从「逐个题型抢救」转成「全科度量 + 堵住共用的闸」（2026-09-14）

用户反馈：「各个题型都有普遍的丢题问题，真题哈。我之前进行了很多轮次的修复，但是显然这个问题仍然普遍。」

结论：**之前几轮修的都是对的，但修的位置太窄。** 丢题的主要机制是一条对所有题型都生效的闸，
而 09-13 那轮只把它给填词（CTW）开了口子；同时**丢题从来没有被度量过**，
所以每一轮都只能等用户撞见某一篇少题，再针对那个题型抢救一次 —— 换个题型照旧丢。

本轮做两件事：① 把那条闸对全部题型放开并让它自愈；② 建一张常驻的全科丢题账本 + 防退化闸。
本轮**不花 token、不改题库数据**：数据侧的回收要在本机跑（见 §5 跑法）。

---

## 1. 先把账算清楚：全科到底缺多少

新工具 `scripts/realbank/loss_ledger.mjs`（零 token、只读仓库里的账本文件，任何机器都能跑）：

```
全库槽位 4327/8280 = 52.3%，缺 4012 题
  管线丢题     585 题   源里有、这一科也跑过，题却没进库 —— 重扫 flagged 块可回收
  整科跑了归零 359 题   管线覆盖这一科、这卷也确实跑过，却颗粒无收 —— 同上，重扫
  整科没跑过  2096 题   管线本来就没跑它（听力/口语）—— 补不补是铺量决策
  源料缺陷     821 题   源 PDF/音频自己就缺，补不了
  复核扣下     114 题   设计行为
  跨卷合并丢弃  37 题   合并判据挡下的
```

**「整科缺席」必须拆成两种，因为处置完全相反。**账本第一版把它们混成一桶，差点把 359 题
误判成「要找商家补料」。两条证据把它们分开了：

1. 管线的 `SECTIONS` 锁死在 `reading,writing`（听力/口语靠单独脚本合流）——
   所以听力/口语整科没题 = 本来就没跑它；
2. **24 套写作整科缺席的卷，阅读全都有题**（听力 42 套里 40 套、口语 31 套里 29 套同样）——
   说明这些卷确实进过管线。阅读/写作在覆盖范围内却颗粒无收，那不是「没跑」，是**跑了丢光**。

再对一次源料体检：99 组整科缺席里，**只有 19 题**能被体检的坏账（section_no_stems /
section_no_answers / answer_key_misaligned …）解释。其余 2436 题，体检看过那一科并没说它坏。
也就是说「缺是因为源里没有」这个假设，数据基本不支持。

| 题型 | 入库/应有 | 完整度 | 管线丢题 | 整科跑了归零 | 整科没跑过 |
|---|---|---|---|---|---|
| ctw 填词 | 1739/2070 | 84.0% | **148** | 40 | 0 |
| bs 造句 | 276/690 | 40.0% | **110** | **231** | 0 |
| lc 对话 | 136/686 | 19.8% | **70** | 0 | 375 |
| lcr 短应答 | 259/1039 | 24.9% | **69** | 0 | 536 |
| interview 面试 | 27/276 | 9.8% | **61** | 0 | 124 |
| lat 讲座 | 310/1100 | 28.2% | 30 | 0 | 612 |
| disc 学术讨论 | 7/69 | 10.1% | 30 | **24** | 0 |
| email 邮件 | 9/69 | 13.0% | 30 | **24** | 0 |
| la 通知 | 116/414 | 28.0% | 16 | 0 | 232 |
| ap 学术段落 | 737/840 | 87.7% | 14 | 20 | 0 |
| repeat 复述 | 210/483 | 43.5% | 4 | 0 | 217 |
| rdl 日常阅读 | 497/540 | 92.0% | 3 | 20 | 0 |

**写作那 279 题（bs 231 + disc 24 + email 24）是本轮最大的认知修正**：它们此前看起来像「要补料」，
实际是跑过却颗粒无收 —— 和那 585 题同一种病、同一种处置。学术讨论现在全库只有 7 题、
是整卷拼齐的唯一瓶颈（`docs/realbank-set-blueprint.md` §三），而这 24 题就躺在管线里。

分母 = 69 套源卷 × 2026 蓝图（`lib/realExam/blueprint.mjs`）。
**之前的口径只数 `sets.json` 里已有的科目**，而 47 套卷的听力、46 套的口语、30 套的写作
在 `sets.json` 里连键都没有 —— 于是「一科都没跑过」被显示成「没缺题」，
全库完整度看起来是 84.8%，实际是 52.3%。账本把这部分补进了分母。

用户说的「普遍」就是这张表：**阅读 86.2%，听力 25.4%、口语 31.2%、写作 35.3%。**
前两轮修的是阅读，所以阅读好；别的科从来没被同样对待过。

---

## 2. 根因：一条对所有题型生效、却只给填词修过的闸

### 2.1 调用预算（`scripts/realbank/model_output.js`）

09-13 查清了 CTW「模型输出无法解析为 JSON」的真因：deepseek-v4-flash 是推理模型，
`max_tokens` 把推理 token 一起算，预算给小了推理会把正文吃光、返回空串。
于是 **ctw 的预算从 16000 提到 32000、超时从 90s 提到 300s**。

但当时的结论是「选择题的失败另有转写/分栏原因，别让它们顺手变贵」，所以
`ap / rdl / lcr / lc / la / lat / 修复轮` 全都留在 16000 / 90s。两条证据说明这个判断不成立：

1. 台账里 **531 次调用顶到 16000**，而 CTW 全部块历史上的调用数到不了这个量
   （每套 3 块 × ~70 套 ≈ 210 块，加两轮重跑也不够）→ 选择题块同样在顶预算；
2. 09-13 那轮 `--only-failed` **没改任何代码**原地重跑，165 个 flagged 块救回 81 块（≈50%）。
   确定性的「模型吐坏 JSON」不会靠重跑救回一半 —— 一半一半是预算/超时抖动的签名。

丢题路径：预算吃光 → 正文空或被截断 → 解析失败 → 块标 flagged → **从不进盲审 → 永远不进库**。

### 2.2 超时被当成系统性失败 → 一块慢，整卷白跑（`structure_set.mjs`）

`classifySystemicFailure` 把「请求超时」判为系统性失败，后果是
**整卷作废、不写产物、退出码 3 → `run_pipeline` 整批停**。
而按实测 ~230 token/s，16000 token 要跑 ~70s，紧贴 90s 的墙 ——
推理越长的块越容易撞上，**恰恰是最该救的那些块在拖垮整卷**。
（09-13 给 CTW 放宽超时时已经写明了这个机制，但只改了 CTW 一个题型。）

### 2.3 合流快照只存一次 → 听力救回来的题会被无声丢掉（本轮新发现）

两个来源的听力/口语合流都靠「从解析器原始产物出发」保证幂等，各自把 structure_set 的产物快照一份：

- 第一来源 `merge_first_source_asr.py` → `<卷>.structured.fs_parsed.json`
- 第二来源 `merge_vendor_asr.py` → `<卷>.structured.parsed.json`

两份都是**第一次合流时存一次，之后永不刷新**。所以：重跑 structure_set 把听力块从 flagged 救成 ok，
只写进 `structured.json`，下一次跑合流会从那份**陈旧快照**重建听力，刚救回来的块原样消失、一声不响。
仓库里对阅读已经有同一个坑的解法（`structured_io.syncReadingToBase`，头注写着
「任何就地修阅读的工具只写 structured.json 的话，下一次重跑合流就会被悄悄冲掉」），
但听力/口语从来没走过 structure_set 重跑，所以没人碰到。
**听力是全库丢题最多的一科，抢救恰好卡在这一步。**

### 2.4 元根因：没有账本

`sets.json` 里其实一直躺着全部证据（每套卷每个槽位 need/got），
但没有任何脚本/测试/后台页把它变成「缺口清单 + 归因」，也没有防退化闸。
所以：修完一个题型，没人知道别的题型缺多少；重建一次库，没人知道数字是涨是跌。
这就是「修了很多轮、问题依旧普遍」的结构性原因。

---

## 3. 本轮改了什么（代码侧，全部有测试）

| 文件 | 改动 |
|---|---|
| `scripts/realbank/model_output.js` | 预算按题型给全（选择题/修复轮 24000，default 也 24000，ctw 仍 32000）；**超时一律由 `budgetOf(maxTokens)` 从预算推导**，不许再手写（手写就会再配出一个撞墙组合）；新增 `retryBudget`；解析失败三分为 空 / **截断** / 真坏格式（`classifyBadOutput` + `isBudgetProblem`）—— 截断以前和坏格式报同一句话，正是 09-13 把 AP 的 70 道题误诊成格式病的原因 |
| `scripts/realbank/structure_set.mjs` | 所有要 JSON 的调用改走 `callForJson`：解析不出且失败签名是预算形，就**加倍预算自动重试一次**（成本只落在本来就已失败的块上，`max_tokens` 是上限不是账单）；选择题调用按题型取预算；merge 写回时带 `freshKeys` |
| `scripts/realbank/failure_policy.js`（新） | 失败分级抽成纯函数。**单块超时从硬失败降为软失败**：只记这一块 error（`--only-failed` 还能捡回来），累计 5 次才判为网络/服务侧故障中止。连接级 `ETIMEDOUT` 仍是硬失败 |
| `scripts/realbank/structured_io.js` | `writeStructured` 同时同步两份合流快照（只同步本次新产出的块，不拿合流结果回灌，幂等不破）；全量重跑路径打印提醒「这卷有合流快照，要重跑合流才生效」 |
| `scripts/realbank/loss_attribution.js`（新） | 丢题归因纯函数：按**额度**扣而不是按标签判（`duplicate_cluster` 这类纯溯源标记不认领任何缺题；`section_gap` 的额度就是 detail 里的「缺 N 题」；`ctw_answer_truncated` 只砍填词）；复核扣下按 scope 折成题数；整科缺席按蓝图补进分母，并按「管线覆盖这科吗 + 这卷跑过吗」拆成「跑了归零」（可扫）与「没跑过」（铺量决策）|
| `scripts/realbank/loss_ledger.mjs`（新） | 账本驱动：`data/realBank/loss-ledger.json`（1150 行缺口 + 123 项可执行补题任务）+ `--freeze` 写 `loss-baseline.json` |
| `__tests__/realbank-{loss-attribution,loss-guard,failure-policy}.test.js`（新）+ model-output / ctw-verify 扩充 | 共 +61 例 |

**防退化闸**：`__tests__/realbank-loss-guard.test.js` 拿 `data/realBank/loss-baseline.json`
卡住「重建一次库，某题型悄悄变少」—— 这正是过去几轮反复踩的坑
（补回更靠前的题让 id 改名、下架静默失效、合并把副本的题一起扔了）。
数字涨了要 `--freeze` 重冻；跌了测试直接红，必须先解释为什么跌。

---

## 4. 预期能捞回多少

账本里 **585 题「管线丢题」+ 359 题「整科跑了归零」= 944 题**，都是重扫 flagged 块的直接目标。
按 09-13 的实测命中率（原地重跑就救回 ≈50%，那还是**没有**加预算、没有自动重试的情况），
再叠加本轮的预算放开 + 预算形自动重试 + 超时不再整卷作废，这批的回收率应当明显高于 50%。

另外 **2096 题**标着「整科没跑过」，全是听力（1755）与口语（341）——
`run_pipeline` 的 `SECTIONS` 锁死在 `reading,writing` 造成的覆盖空白。
这一桶要先分清源料：BACKLOG 记着「听力无音频 18 套 = 源缺」，剩下约 21 套听力有音频。
**这一桶没动，因为它涉及要不要给听力/口语铺量、花多少钱，是你拍板的事**（见 §6）。

---

## 5. 本机怎么跑（数据侧回收）

云端这台容器没有 `.codex-tmp/realbank/`（中间产物只在你本机），也没有 API key，所以数据侧一步都没跑。
本机按这个顺序：

**不用自己从账本里挑卷** —— 作业单会按阶段排好可粘贴的命令：

```bash
node scripts/realbank/loss_ledger.mjs --plan            # 全部（写作 61 套 / 阅读 23 套 / 听力口语 23 套）
node scripts/realbank/loss_ledger.mjs --plan --limit 8  # 每阶段只出前 8 套
```

阶段顺序是按**验证价值**排的，不是按缺口大小：写作（不碰音频、链路最短、最卡整卷）→
阅读（量最大）。同一套卷的多科会合成一条命令。

**听力/口语不在作业单里**（那 250 题），原因见 §7 —— 重扫它们既白花钱又污染产物。

**第 0 步是零成本的**：作业单里每条命令都带 `--dry`，`--dry` 在任何模型调用之前就返回，
不花一分钱、秒回，只报这卷这科还有几个失败块可重扫。所以先把整个阶段的 `--dry` 跑一遍，
拿到真实可回收量，再决定花钱跑哪些。怎么读结果：

| `--dry` 说 | 含义 | 下一步 |
|---|---|---|
| 待处理题块 N | 有 N 个失败块可重扫 | 去掉 `--dry` 真跑 |
| 没有需要处理的块 | 这卷这科在中间产物里**根本没有题块** | 缺口在 ingest/对齐层，重扫解决不了；**记下来**，这是有用的诊断 |
| 需要既有产物 | 这卷的中间产物不在 `.codex-tmp` | 跳过 |

第二行尤其要留意：写作那 449 题里有多少是「块都没有」，只有 `--dry` 跑完才知道 ——
这正是把「跑了归零」进一步分成「跑了丢光」与「压根没解析出块」的唯一办法。

跑完扫描后的收尾（顺序不能换，作业单末尾也会打印）：

```bash
python scripts/realbank/merge_first_source_asr.py --all   # 只有扫过听力/口语才需要
node scripts/realbank/build_bank.mjs
node scripts/realbank/assemble_sets.mjs
node scripts/realbank/loss_ledger.mjs --freeze            # 数字涨了就重冻基线
npx jest __tests__/realbank-loss-guard.test.js            # 确认没有哪个题型反而变少
node scripts/ops/deepseek-usage-report.mjs                # 对账这轮花了多少
```

花费：只对已经失败的块重跑，且只在失败签名是预算形时才多一次重试。

**顺手可验证 §2.1 的推断**：台账里顶到 16000 的那 531 次调用，按题型拆一下
（`.ops/deepseek-usage.jsonl`）。如果非 CTW 占大头，就实锤了「选择题也一直在顶预算」。

---

## 6. 建议（按做的顺序）

### 第一步：先把 944 题捞回来 —— 不需要拍板，且第 0 步零成本

`node scripts/realbank/loss_ledger.mjs --plan` 出作业单，先把**阶段 1 写作**的 `--dry` 全跑一遍
（不花钱、秒回），拿到真实可回收量，再去掉 `--dry` 真跑。

为什么先写作：不是它最大，是它最卡 —— 学术讨论全库只有 7 题、是整卷拼齐的唯一瓶颈，
而这一桶里就躺着 24 题 disc + 24 题 email；且写作不碰音频、不用重跑合流，链路最短，
最容易看出新预算是不是真的在救题。写作跑通、确认日志里「预算重试」确实救回东西，
再铺到阅读（填词为主）和听力。

### 第二步：听力/口语铺量 —— 建议**先只铺一套试水，不要整批铺**

2096 题里听力占 1755。别急着整批跑，原因是成本结构不对称：文本生成走订阅边际≈0，
**听力的钱全在 TTS 配音**，而且 `run_pipeline --all` 有已知的烧钱坑（会把月份文件夹当卷跑）。
建议：挑 1 套**确认有音频**的卷，走 `structure_set --sections listening,speaking` +
合流 + 配音，实测单套花多少、盲审过多少，再拿这个数字决定剩下 20 套铺不铺。
一套的代价是几块钱，买到的是一个能外推的单价。

### 第三步：`SECTIONS` 放开 —— 建议**现在不动**

放开等于把听力纳入常规产线：每次铺量自动带上听力与 TTS 成本，而 `--only-failed` 直接调
`structure_set --sections listening,speaking` 已经够用，放开只省几行命令。
等第二步的实测单价和盲审通过率出来，再决定要不要把它变成默认。

（`AP over_cap 11 道`是 09-13 的残余项，与本轮无关，仍挂 BACKLOG。）

## 7. 已知未决（本轮没碰）

- `__tests__/realbank-artifacts-sync.test.js` 有 **2 例在 HEAD 上就是红的**（upload 失败没收敛成 `SyncFailure`，
  抛的是普通 `Error`）。与本轮改动无关，已确认 stash 掉全部改动后照样红。没修，免得混进这次的 diff。
- 账本对「整科缺席」只能看出「一题都没进过库」，分不清「源里没有」和「没跑过」——
  要分清得读 `.codex-tmp` 里的对齐产物，那只在本机。本机跑一次账本可以顺手补这一维。

---

## 7. 更正：听力/口语不能靠重扫回收（2026-09-14 二次核查）

用户看到第一版作业单后指出「听力不用重扫吧，我们不是把商家给的听力直接放进题里吗」。
核过代码与数据，**这个更正是对的，而且实际后果比「没必要」更严重**。

### 7.1 听力题到底是怎么拼出来的

以 `real_lat_121b_1_25` 为例，逐字段的来源：

| 字段 | 来源 |
|---|---|
| `audio_url` | **商家原声**（`audio_source: "original"`，存 Supabase `listening_audio` 桶）。全库 440 条里 **388 条挂原声**，52 条退回 TTS |
| `transcript` / `turns` / `speakers` | **商家「听力原文」逐字稿 PDF**，经 Whisper 词级对齐核验（该条 `asr_similarity` 0.996）。合流层产出 |
| `questions[].stem` / `options` | OCR 的答题屏 → `structure_set` 模型转写。**这是 structure_set 唯一负责的部分** |
| `answer` | 答案 PDF 的答案键（模型只转写不解题） |

所以「商家给的听力直接放进题里」对音频与逐字稿完全成立；模型只经手题干和选项。
退回 TTS 的那 52 条，原因全是音频定位类（`locate_failed` 23 / `anchor_mismatch` 11 /
`coverage_low` 7 / `tail_not_clean` 4 / `wpm_out_of_range` 3 / `source_no_speech` 3），
没有一条是「模型转写失败」。

### 7.2 重扫会发生什么（实测判据，不是推测）

`build_bank.mjs` 对听力有一句 `if (!st.merged_asr) continue;` —— **没跑过合流的卷一条听力都不收**。
也就是说听力的产物完全由合流层拥有。而两边的块 key 格式**零重合**：

- `structure_set`（`collectUnits`）：`section|module|start-end|total` → `listening|1|13-14|32`
- 合流写回（两个来源都是）：`listening|{mod}|{q_start}` → `listening|1|13`；
  口语是 `speaking|1|repeat|1` / `speaking|1|interview|1`

于是在合流过的卷上跑 `--only-failed --sections listening`：

1. 按 key 一条既有记录都查不到 → 每个块都被当成「没跑过」→ **不是只扫失败的，是整科全量重跑、全额付费**；
2. 跑出来的结果 key 也对不上，`--merge` 回写只能**追加** → 产物里同一段听力出现两份
   （合流那份 + 生料那份），直到下次跑合流整体覆盖才清掉。

而且重跑换不来题：听力扣题的判据（`transcript_mismatch` / `diarization_failed` /
`group_count_mismatch` / `transcript_truncated` / `screen_items_missing` …）**全部判在合流层**，
`structure_set` 只看 OCR 文本、根本不碰音频。

### 7.3 已改

- `failure_policy.isMergeOwned`：合流跑过的卷，`--only-failed` **直接不给听力/口语派活**，
  并打印为什么、以及该去哪修。（上一版加的 `merged_by` 跳过是**无效的** —— 正是因为 key 查不到，
  那个判断根本不会触发。这是我上一个 commit 的错，已在此更正。）
- `--plan` 作业单删掉听力/口语阶段，改成单独一段说明「这 250 题不要重扫」+ 正确的修法入口。

### 7.4 听力那 250 题该怎么补

回合流层，按 BACKLOG 既有的线：

```bash
python scripts/realbank/lc_gender_worksheet.py --list --csv lc-gender.csv   # 听音标性别（对话回收率第一瓶颈）
python scripts/realbank/lc_gender_worksheet.py --apply lc-gender.csv
python scripts/realbank/merge_first_source_asr.py --all
```

源料本身缺的（音频缺失 / 无说话人标签 / 1.28A 听力 mp3 实为 1.21A 的）要找商家补料，
清单在 `data/realBank/listening/original-audio.json` 的 `skipped`。

---

## 8. 再更正：写作那 449 题里，只有 108 题能靠重扫（2026-09-14 三次核查）

写 §6「先跑写作」时没核 BS 的题面来自哪里。核过 `build_bank.buildWriting` 后：

| 题型 | 可回收 | 题面来源 | 怎么补 |
|---|---|---|---|
| email | 54 | `structure_set` 转写答题屏 | **重扫**（`--only-failed --sections writing`）|
| disc 学术讨论 | 54 | 同上 | **重扫** |
| bs 造句 | 341 | `extract_bs_pages.py` **看图**产出的 `<卷>.bs.json` | 看图，不是重扫 |

原因：第一来源的写作 PDF **没有文字层**，造句题的模板 + 乱序词块只存在于考试界面截图里。
`structured.json` 的 `build` 段只有 `{n, sentence}` 答案句，`build_bank` 按 thin 丢弃
（源码注释写着「单靠它拼不出可练的题」）。所以对造句跑 `--sections writing` 是空转
（`build` 走零 token 路，不花钱，但也不产题）。

**造句那 341 题还卡着一条未决项**（BACKLOG 已记）：写作 PDF 上的词块边界被 OCR 糊掉，
363 条只能做成「真题句子 + 本站切块」—— 这种来源分档接不接受要先拍板，别先烧看图的钱
（`extract_bs_pages.py --dry-run` 会先报「几张图 / 预计 ¥」，¥0.01/张）。

好消息是剩下的 108 题正是最卡的那部分：**学术讨论全库只有 7 题**，是整卷拼齐的唯一瓶颈，
而这里有 54 题；邮件同理（14 → 可能 60+）。而且写作**不走盲审闸**
（`buildWriting` 只看 `status === "ok"`），重扫完直接 `build_bank` 就能进库，链路最短。

阅读则相反：`build_bank` 的阅读闸按 `<卷>.audit.json` 放行，**救回的题必须补盲审**
（`audit_answers.mjs "<卷名>" --section=reading --only-missing`），否则当场被丢
（`stats.droppedNoAudit`）。作业单末尾已加这一步，并标了 `--only-q` 不带 `--only-missing`
会清空该卷阅读盲审条目的工具坑。
