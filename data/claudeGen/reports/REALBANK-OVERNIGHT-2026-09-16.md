# 真题专区补题 · 过夜账（2026-09-16）

> 口径：全库槽位一律取 `node scripts/realbank/loss_ledger.mjs --dry-run` 的「全库槽位」。
> 钱：DeepSeek 取 `.ops/deepseek-usage.jsonl`（¥5.24/M tokens 口径），千问识图 ¥0.01/张另记；**TTS 全程 ¥0**。

## 总账

**全库槽位 5166 → 6403（+1237），62.4% → 77.3%。花费 ¥10.84（DeepSeek ¥10.05 + 千问 ¥0.79），TTS ¥0。**

| 阶段 | 槽位 | DeepSeek | 千问 | 提交 |
|---|---|---|---|---|
| 起点 | 5166 / 8280（62.4%） | — | — | — |
| A 造句 `bs_recover` | +0 | ¥0 | ¥0 | 无改动 |
| B 3.16 pilot（新建合流器） | 5166 → 5210（+44） | ¥0.73 | — | d77027e5 |
| C 第 1 批（3.18/3.17/4.8/3.20/4.20） | 5210 → 5356（+146） | ¥1.02 | — | c3f554a9 |
| A 补：5 月第二来源 6 套造句 | 5356 → 5412（+56） | ¥0 | ¥0.73 | 4b58b85a |
| C 第 2 批（4.13/4.27/4.28/5.3/5.6 + 第 1 批重跑） | 5412 → 5638（+226） | ¥1.80 | — | a07b149f |
| C 第 3a 批（4.15/5.10） | 5638 → 5714（+76） | ¥0.41 | — | 0991f07f |
| D ① 重扫（1.21A 填词试探） | +0 | ¥0.07 | ¥0.06 | 无改动 |
| C 第 3b/4 批（+11 套） | 5714 → 6141（+427） | ¥1.99 | — | 13512e30 |
| C 第 5 批（5 月第二来源 5 套 + 4.5） | 6141 → 6313（+172） | ¥0.87 | — | 123eb7f1 |
| C 第 6 批（3.10 / 3.6，录音从第 4 / 第 9 题开始） | 6313 → 6370（+57） | ¥0.44 | — | 123eb7f1 |
| C 第 7 批（5.29，两段录音拼成一条） | 6370 → 6403（+33） | ¥0.07 | — | 8fd2efdc |

分科：听力 943 → **2124**（29.1% → 65.5%）、写作 683 → 739（82.5% → 89.3%）、阅读 3245（未动）、口语 295（未动）。

## A. 造句

`bs_recover.mjs --run`：**+0**。333 页识图缓存早已全覆盖，缓存复验结果与库里逐字节相同。

账本里造句「管线丢题」60 题实际卡在 5 月第二来源 6 套 docx 卷（5.6_v2 / 5.10_v2 / 5.11 / 5.18 / 5.23 / 5.29）：
答案页写作段是「一段一句、**不编号**」，ingest 的答案解析器一条都认不出 → structured 写作段为空；
识图脚本又只扫桌面源根目录，看不见 `src-converted/` 里转出来的写作 PDF。

修法：`extract_bs_pages.py` 加 `docx_writing_answers` 兜底（顺着 convert_docx_set 的转换记录读原始答案.docx，
取「写作」表头到下一个科目表头之间**恰好 10 段**，顺序即题号；这类卷的同卷全扫候选池就用这 10 句）。
识图 73 张 ¥0.73，通过 56/60 → **bs 558 → 614**。配 `__tests__/realbank-bs-docx-answers.test.js`。

## B. 3.16 pilot：现有链路接不上 → 补上缺的一环

- `merge_vendor_asr.py` 只认 rf/rp（商家逐题 mp3，按文件名分组）；
- `merge_first_source_asr.py` 要「听力原文」PDF + ListeningModule1/2.mp3；
- 这 38 套数字卷**每套只有一条「听力.m4a」整场录音**，两者都没有；`build_bank` 只收带 `merged_asr` 的卷 → 一题都进不了库。

新增 `scripts/realbank/merge_recording_asr.py`（jest 接线：`__tests__/realbank-recording-merge.test.js`）：
本机 faster-whisper（medium.en 词级时间戳，VAD speech_pad=1s，零 API 费用）转写整条录音 → 按 ≥4s 静音切岛 →
ETS 旁白定题材 → 岛序列切 module → 与 2026 蓝图逐段比题材 + 屏幕总题数校验和 → 分完段**完全复用**
`merge_first_source_asr.build_listening` 的全部闸；对话逐句基频分角色，再加一道「题干引原话反查角色」的语义闸。

配套改动：
- **修 `build_bank --only-audio` 两个潜伏 bug**：漏音频沿用 / 原声回挂（跑一次全库听力 audio_url 清空：388 原声 + 68 TTS），漏面试拆套（45 → 30）。修完在现有库上逐字节无变化。
- 整块录音来源的题**没挂上原声不收**（`lDroppedNoOriginalAudio`）；先落库再切片时带 `--keep-unbound-recording`。
- `bind_original_audio --cached-narration-only`：旁白只用已缓存句子、缺的退回通用句，**一次 TTS 都不调**。

**单套实耗**：结构化 ¥0.65 + 盲审 ¥0.08，约 10 分钟（含本机转写 2 分钟）。

## C. 批量（33 套录音卷）

驱动 `scripts/realbank/recording_batch.mjs`：**先转写 + 查重、只把非重复题组送结构化**（实测省约 45%），
逐套记钱与耗时、超预算自停；整批落库时所有录音卷按卷名顺序重合流（查重先后稳定），只给「清单里没有 / 口播变了」的条目切原声。

**产出**：这批卷自己的新题 **226 条**（LCR 161 / 对话 38 / 通知 13 / 讲座 14），全部挂上真人原声（226/226）；
另有 **463 条**是与库里已有题重复 → 只记别名还回原卷槽位，不重复收。
听力盲审总一致率 **425/505 = 84.2%**（LCR 98%、对话 85%、通知 70%、讲座 66%）；不一致的按规不上线。
抽查过的低分项（3.16 讲座 7/15）逐题核对：模型答案与录音原文吻合，**是商家答案页给错**（例：构造干涉被答成「只从外表面反射」）。

### 逐套跑出来的录音毛病与判据（修完用已转写的 33 套回归，零误伤）

| 现象 | 卷 | 判据 |
|---|---|---|
| 开场音量提示的多种说法 / 长达 71 词 | 4.8、3.20、2.25 | 补进开场白识别；上限 60 → 120 词 |
| 一两个词的孤岛（ASR 残片）被当成一句 LCR | 5.10_v2 | ≤2 词的岛当噪声丢（真题最短刺激句 3 词） |
| LCR 首词时间戳错位自成一岛 | 4.29、5.3 | 没说完的尾巴（≤3 词 + 下一岛小写开头）挪到下一岛 |
| 旁白与正文之间没转写出句号 | 4.8 | 按停顿切，停顿前那几个词得是完整旁白；切不出宁可不切 |
| 旁白被错挂到上一岛尾巴 | 3.16 | 挪回下一岛；下一岛自带旁白就不挪（防通知丢末句） |
| 录音暂停切开、内容完整 | 4.11 M1（与 4.8 同条逐字比对一字不差） | 句末收尾 + 大写开头 + <8s 才并回，留痕 `pause_joined` |
| 录音断流、半句被切断 | 4.11 M2、3.15 | 不并 → module 对不上蓝图 → 扣下 |
| 「录音 module 数 ≠ 屏幕」一刀切 | 4.11 | 改逐 module 独立校验（否则完好的 M1 被连坐） |
| 正文缺失 / ASR 幻觉循环 / 远短于真题下限 | 4.11 M1 的通知只剩 33 词 | 按真题分布定下限（通知 40 / 讲座 150 / 对话 45 词），单组扣下 |
| 整段材料没录上 | 5.3 M1（三段对话后 97s 空档，正常 ≤40s） | 唯一异常空档处插占位，占位组扣下、其余照常 |
| 录音与屏幕都从第 N 题开始 | 3.10（Q4）、3.6（Q9） | 两边**恰好对得上**时前面补空位 LCR，题号自然对齐 |

**structure_set 修复轮串题（新发现，已加闸）**：「合并邻块重切」经常把相邻题的题干串错位 ——
成对重复里总是「修复过的 Qn 盲审一致、紧挨着没修复的 Qn+1 不一致」。盲审能拦大部分，但题干配错材料时模型等于瞎选、
有 1/4 概率恰好等于答案页而漏网。合流新增：同组题干逐字相同剔没修复的那份；修复过的题干关键词不在本组材料却在同 module
别组 → 剔。6 套回测剔 14 题，13 题本就盲审不一致，唯一「一致」的那题正是题干抄重、答案字母碰巧相同的漏网题。

**4.29 首跑没落库**：它从没生成过盲审文件（阅读只有填词），驱动的 `--only-missing` 报错；`build_bank` 又把 `dup_of` 别名
放在盲审闸里面，连 22 组重复题的别名一起丢。两处都修（没审过就整科审；别名移到盲审闸外）。

## D. ① 管线丢题重扫：几乎无可回收

`structure_set --only-failed --dry` 逐项核过账本 tasks[]：
- **听力 59 / 口语 22**：全在已合流的卷（第一来源 / rf·rp），结构化层面「归合流所有」，重扫一块都不会跑；病在合流层。
- **写作**：rf 卷走 parse_reformatted，structure_set 没有块可扫；5 月 6 套造句已在 A 补里用 docx 兜底收回。
- **阅读 19**：可重扫的 flagged 块只有 8 个，6 个是源料本身缺选项 / 答案不是字母（点选句子类）；
  **1.21A 填词 M1 1-10 看图转写后重扫仍拒收 —— 截图原文「they ca_ out val_ and fjords, lea_ behind」，
  答案页却写 cover / living，是商家答案页印错（应为 carve / leaving）。改答案页属人工核定，未擅动：确认后可 +10 题。**
  本项试探花 千问 ¥0.06 + DeepSeek ¥0.07。

## 跳过 / 未做（附原因）

| 卷 / 项 | 原因 |
|---|---|
| 3.4、2.23、3.8 听力 | 源料体检标 blocking（`section_gap`：屏幕/答案页整个 M2 缺失，缺口 ≥10 题按规须人工核过才入库） |
| 4.5 M1、5.6_v2 M1、3.2A M1 | 录音里 LCR 缺几句、看不出缺的是哪几句（正常间隔 ~15s，异常空档折算 3~4 句，界线不清）→ 不猜，只收 M2 |
| 4.11 M2、5.11 M2、3.15 整卷 | 录音断流：对话 / 讲座每隔 8~11s 掉一截（中间明显缺词）→ 扣下 |
| ~~5.29 听力~~ | 已做：带 part 号且连续的多段录音自动 ffmpeg 拼成一条（时间轴连续，切片与词级时间戳同一条轴） |
| 5.3 M1 的第 4 段通知、4.11 M1 的第 3 条通知 | 录音里整段缺失 / 只剩后半截 → 该组扣下，同 module 其余题照收 |
| **口语 ③（29 套 319 题）** | `bind_original_audio` 只会切听力（lcr/lc/la/lat）；口语库现有 333 句复述 + 174 道面试的音频全是 TTS 配的。本轮约束「不许付费 TTS、切不出原声就不上线」下，口语新题**没有音频来源** → 要先给 bind 加口语切片（复述句 / 面试题按词级时间戳从口语整段录音里切），另立项 |

### 原声没切出来的零头

每轮落库都会重试「清单里没有」的条目，其中 6 条 LCR 一直过不了闸：5 条 `wpm_out_of_range`（切片把作答留白算进去了，6 个词的刺激句算出 16~24 wpm）、1 条 `tail_not_clean`。按规不上线，留作下次调 `resolveTail` 判据时的样本。

## 需要人来做

1. **推送**：GitHub 凭证要交互登录（Git Credential Manager 弹窗；`gh` 未登录），无人值守推不上去 —— **本夜 9 个提交都在本地 main**（d77027e5 / c3f554a9 / 4b58b85a / a07b149f / 0991f07f / 13512e30 / 123eb7f1 / 8fd2efdc + 一个 merge），
   桌面上点一次授权（或 `gh auth login`）后 `git push origin HEAD:main` 即可。
2. **1.21A 填词答案页订正**（+10 题）：确认按截图把 cover / living 改成 carve / leaving。
3. ~~**口语铺量拍板**~~：已立项并做完（见文末「续」），319 题回收到只剩 77 题。
4. 发版公告未写（按约定不碰 CHANGELOG / announcements）。

---

# 续：口语原声切片立项（09-16 白天，用户「口语切片立项做」）

> 口径同上。本段起点 = 过夜收官的 6403，终点 **6705（81.0%）**，新增花费 **¥0.11**（DeepSeek），TTS 仍 ¥0。

| 阶段 | 槽位 | 花费 | 提交 |
|---|---|---|---|
| 一期 存量口语换真人原声 | 6403（不变，只换音频） | ¥0 | afccc2ce |
| 二期 数字卷新复述题 16 套 | 6403 → 6507 | ¥0 | ef03f7d9 |
| 3.2A 的 M1 认回来 | 6507 → 6541 | ¥0.11 | a2120b05 |
| 口语别名顺序修正 + 全量重切原声 | 6541 → 6588 | ¥0 | f320a0eb |
| 面试题干收 ASR + 四道机械闸 | 6588 → 6669 | ¥0 | 67f964e2 |
| 没有答案页的卷也收面试题（含 8 套 mp4 录屏） | 6669 → **6705** | ¥0 | 5bc0d0ce |

**分科终值**：阅读 3245/3450（94.1%）· 听力 2157/3243（66.5%）· 口语 **564/759（74.3%）** · 写作 739/828（89.3%）。

**账目更正**：a2120b05 的提交信息写「6528 → 6607」，6607 是误读 —— 当时 `sets.json` 与题库不同步。
事后 `git stash` 复核，该提交实际落在 **6541**。本表按复核值。

## F1. 一期：`bind_original_audio` 学会切口语

原本它只切听力（lcr/lc/la/lat）—— 口语库 333 句复述 + 174 道面试的音频**全是 TTS 配的**。

- `original_audio.js` 新增 `SPEAKING_SUBITEMS`：口语按**子条目**切（一套复述 7 句各一条 mp3），
  原声清单按 `real_repeat_rf0610_1_s3` 这种子条目 id 记。
- `bind_original_audio.mjs` 新增口语索引与计划：商家逐条切好的（rf/rp）直接用那条 mp3，
  只有整段录音的（第一来源 / 数字卷）按词级时间戳从整段里定位切。
- **坑**：复述与面试共用同一条口语整段录音、两边都从 1 编号，单调游标会跳进面试段再也回不来
  （实测第一来源 63 句只过 8 句）→ 面试整体加 1000 偏移，随后 57/63。

结果：507 个子条目进计划，**475 条过闸上传（93.7%）**，TTS 从 507 条降到 32 条。抽 6 条切片重新转写，与库里文本 sim = 1.00。

## F2. 二期：数字卷的新复述题（**DeepSeek 调用 0 次**）

`merge_recording_asr.py` 加 `--speaking`：口语整段录音本机转写 → 复述句定稿 → 复用 `build_speaking`。
与听力那半段互不相干（听力最贵的是逐句基频判角色，口语不必陪跑）。
复述句走 `structure_set` 的**零 token 路**（答案页里就是句子本身），所以整条链路一分钱没花。

**定稿口径改了一处，这是本段唯一的判断题**：跟读题的刺激物就是那段音频，判分是拿考生的 STT 逐词比这句话 ——
句子与音频不一致，说对的人反而被扣分。而数字卷的答案页是商家手打的转写，实测 3.18 七句错三句
（tire 打成 tier 两处、pen 打成 pin、"reattach the wheel tightly back on" 漏成 "…back to"），且整批全小写无句末标点。
于是：

- 逐句按**单调游标**在录音里找对应的那一句，相似度低于 0.6 判作没找到 → 不动（ASR 只能改已被文档证明存在的那一句的字面，不能凭空加一句）；
- 差异在预算内（短句 ≤1 词、长句 ≤2 词）→ **以录音那一句定稿**，`problems` 里记 `repeat_text_from_audio`；
- 差太多但答案页**说到了录音这一句的结尾** → 内容仍用答案页的，只补首字母大写与句号；
- 答案页真被换行截半句（后面还有几个词没说到）→ 不动，按老规矩 `sentence_truncated` 扣下。

26 套合流 **174/179 句可落库**（5 句是 ASR 与答案页差太多，fail-closed 扣下）。
落库时口语也过「切不出真人原声就不上线」这道闸（新 drop 码 `sDroppedNoOriginalAudio`，与听力同口径）。

**11 套是商家跨日期重复用的同一份口语**（3.17=3.15、3.4=1.21A、4.11=3.2B…）→ 记别名，不重复落库。

## F3. 顺手治好的三个病

1. **开场提示粘着第一句短应答**（3.2A）：第 1 句 LCR 跟在音量提示后面只隔 2.9s，切岛（≥4s 静音）看不见这条缝，
   整句被并进开场提示岛 → M1 只数出 11 句 LCR、对不上蓝图、**32 道题全丢**。
   新增按句切开，四个条件缺一不可（前半截每句都是开场提示 / 后半截是说完的一整句 / 中间 ≥1.5s 停顿 / 长度像 LCR）——
   先只按「短句 + 开场提示」切的那版，把 3.30 / 4.15 / 4.27 / 5.23 四套被 ASR 截半的提示残片（"You now have the…"）
   也切了出来，多一句 LCR 反而把本来认得出的 M1 顶坏。**全库只有 3.2A 一套触发。**
2. **原声闸放错了位置**：口语的原声闸原本放在去重之前，整套被闸空的重复卷连**别名**都记不上 ——
   那一卷的槽位反而更空（保留方明明有内容）。实测 8 套这么丢的。改成「validator → 去重记别名 → 原声闸 → 再验一次」。
3. **4.5 M1 / 5.6_v2 M1 仍然不收**（各 32~34 题）：复查确认是**源料缺**而非管线病 ——
   5.6_v2 的录音在 31s~98s 之间整整 67 秒没有任何语音，正好是 2~3 句 LCR 的位置；
   按正常 15~18s 间隔折算，缺 2 句还是 3 句算不出唯一解 → 不猜，保持扣下。

## F4. 当前口语/听力的音频构成

| | 条目 | 真人原声 | TTS |
|---|---|---|---|
| 听力（lcr/lc/la/lat） | 685 | 631 | 54 |
| 复述（按句） | 428 | 406 | 22 |
| 面试（按题） | 174 | 166 | 8 |

## F5. 还缺什么

- **面试题 ③（7 套 77 题里的大头）**：面试的 4 道题干**只在音频里**——屏幕只印场景（"You have volunteered for a
  research study…"），答案页只抄复述句。现行口径是「文档没有就 hold，不拿 ASR 顶」，所以整组扣下。
  **要不要改这条口径，是本轮唯一需要人拍板的事**，见文末。
- **14 套答案页没有复述句**（`section_no_stems` / 无答案页）：没有锚就不能上，源料问题。
- **5.29 的一条短应答** `real_lcr_529_2_01`：全量重切后尾巴不干净（`tail_not_clean`，切片会带到下一题的开头），
  按规不上线 → 基线里 lcr 762→761，是**有意调低**。

## F6. 面试题干：已拍板收 ASR + 四道机械闸（6669）

用户 09-16 拍板「收」。落地后 **6588 → 6669（80.5%）**，口语 447 → 528 槽位（69.6%），DeepSeek 仍 ¥0。

四道闸（缺一不可，任一不满足整组扣下）：

1. 录音里认得出「Take an interview / An interviewer will ask you」这段固定旁白；
2. 旁白之后**恰好 4 段带问号的内容** —— 按 ≥4s 静音切段，每道题后面都有考生的作答时间，停顿天然把 4 道题分开；
   第 1 题常与旁白同段（间隔不到 4s），按句从第一句带问号处起算。26 套里 24 套分得出 4 道，
   2 套（3.25 / 4.20）ASR 把问句转糊了 → 整组扣下；
3. 每道题剥掉面试官的应答词后仍带问号、5~60 词、以句末标点收尾。
   **剥的规矩**：只从前面剥、只剥**没有问号**的整句，且必须命中三张闭合词表之一
   （ETS 固定指令 / 场景屏朗读 / 「谢谢参加」类寒暄与「我想问你几个关于…」类铺垫）。
   带问号的句子和「Some people believe that …」这种**前提句一个字都不剥** —— 前提剥掉题就残了；
4. 每道题都要能从同一条录音里切出真人原声并过闸（`bind` 的覆盖率 / 锚点 / 语速），切不出的不上线。

结果：**96 道题干 93 道过闸（96.9%）**（3 道是 ASR 把两段话连读、词数 61~105 超上限）；
切片 232/243 过闸上传。面试现在 62 套 239 题，**231 题真人原声，其中 65 道题干来自 ASR**（库里立了 `from_asr` 旗）。
场景屏优先取**屏幕块**（文档来源），屏幕被 OCR 糊掉时才退到录音里那段朗读 —— 场景只展示不判分。

## F7. 没有答案页的卷也收面试题（6705，口语 74.3%）

面试题干本来就不靠答案页 —— 于是把「structured 里没有口语产物就跳过」这条前置去掉：
16 套答案页没有复述句的卷（`section_no_stems` / 整张答案页缺失）照样跑面试。
顺带发现 8 套的口语源料是**录屏 .mp4**（2.8 / 2.23 / 3.6 / 3.8 / 3.11 …，最大 228 MB），
`speaking_recordings` 原本只认音频扩展名 → 一条都找不到；口语这半段加认 .mp4/.m4v/.mov
（ffmpeg 一样能读，切片与词级时间戳不受影响），听力那半段不动。

剥离词表按实测补齐了 ETS 固定指令的 ASR 变体（"A block will indicate…" = 听岔的 "The clock"、
"No type of preparation" = "No time for preparation"、"As part of a university project…"、
"A graduate student conducting the research…"）。

结果：**36 套 144 道题干，141 道过闸（97.9%）**；面试库 69 套 267 题，**259 题真人原声、93 道题干来自 ASR**。
口语槽位 528 → **564（74.3%）**，「整科没跑过」只剩 **2 套 22 题**。

收不下的 4 套各有各的源料问题（都 fail-closed 扣下，没猜）：
2.25 / 3.30 录音里根本没有面试旁白；3.2A / 5.23 旁白后只分出 3 段；5.3 分出 5 段。

## 需要人来做（续段新增）

~~**面试题干要不要收 ASR 的**~~ —— **已拍板收，已做完，见 F6**。原文存档：

- 现状：面试题干没有任何文档来源 —— 屏幕只印场景，答案页只抄复述句。按现行口径整组扣下。
- 支持收的理由：① 这批卷的听力 lc/la/lat 正文**本来就是 ASR 转的**（整块录音卷没有逐字稿），口径上已经开过口子；
  ② 面试是开放题，没有客观答案，一个词转错的代价远小于选择题；
  ③ 可以给每道题切出真人原声，`bind` 的覆盖率 / 锚点 / 语速三道闸会机械验证「这段文字确实是这个位置说的」。
- 反对收的理由：口语不走盲审（无客观答案），收 ASR 就等于**没有第二票**；现行口径白纸黑字写在
  `merge_first_source_asr.build_speaking` 的注释里，是当初有意定的。
- 我的建议：**收**，但加四道机械闸 —— 录音里认出「Take an interview…」旁白、其后恰好 4 句、每句以问号收尾、
  每句都能切出原声且过闸；任一不满足则整组扣下。
- ~~未经拍板不动。~~ 用户选了「收，加四道机械闸」，已按此实现。

---

## 收尾：这一轮到此为止（自动能做的做完了）

**全库槽位 5166 → 6705（62.4% → 81.0%），累计花费 ¥10.95（DeepSeek ¥10.16 + 千问 ¥0.79），TTS 全程 ¥0。**

剩下的四类都需要人，脚本再跑也不会涨：

1. **推送**：GitHub 凭证要交互登录（`GIT_TERMINAL_PROMPT=0` 下直接报 could not read Username；`gh` 未登录），
   **16 个提交全在本地 main**。桌面上点一次授权或 `gh auth login` 后 `git push origin HEAD:main`。
2. **听力 6 套 109 题**：2.23 / 3.4 / 3.8 的 M2 整个缺（屏幕与答案页都没有），源料体检标了 `section_gap` blocking ——
   按规「缺口 ≥10 题须人工核过才入库」；3.15 录音断流；1.21A 差 4 题。要人核过才能放行。
3. **1.21A 填词答案页订正**（+10 题）：截图原文是 `carve / leaving`，答案页印成 `cover / living`。
4. **口语最后 22 题**：2.25 / 3.30 的录音里根本没有面试旁白（源料就没录上），没有可回收的东西。

发版公告按约定没碰（CHANGELOG / announcements.json 一个字没动）。

---

# 续二：听力补题研究（09-16 下午）

> 起点 6705，本段终点 **6732（81.3%）**，花费 ¥0.72。

## G1. 听力 1086 道缺口的真实构成（逐桶核过，不看账本标签）

| 桶 | 题数 | 实情 |
|---|---|---|
| A 8 套听力**从没跑过** | 上限 ~186 可落 | 5 套结构自证通过；4.24 / 3.24 两个 module 都认不出；3.29 答案页没有听力答案 |
| B 源料真缺 | ~400 | 3.8 的 M2 整缺、3.15 的 part1 没下载完（`.baiduyun.p.downloading`）、2.8 等 |
| C 盲审不一致 + 成对连带 | 108 + 30 | 第二票机制已接好（`hold_policy.auditPassed` 认 `second_vote`），**听力从没跑过** |
| D 对话角色判不出 | 72（36 组） | 15 组的题干本身已点明「the man + the woman」 |
| E 复核扣下 | 61 | 人已决定 |
| F 模块认不出（4.5 / 5.6_v2 / 4.11…） | ~150 | 已核实是录音缺句，缺 2 句还是 3 句算不出唯一解，不猜 |

## G2. 三个可执行的口子（其中 ① 已做）

**① 4.6 单套（已做，¥0.72）** —— 没有 blocking 标记、M1/M2 都自证通过（录音标注「缺q1」，屏幕也是 46/47，
`pad_leading_lcr` 正确补位）。结构化 30 块 + 盲审。**落库 27/47，全库 6705 → 6732。**
盲审 20/26 = 77%，但 **LCR 6/6、LC 4/4 全对** —— 对齐信号干净，不一致的 6 道全在 LA/LAT（模型本身难的那两类）。
它同时是 ② 的活样本：46 道配上答案的题里有 19 道是被盲审不一致连带掉的。

**② 听力跑第二票（未做，估 ¥1）** —— 150 道不一致题用更强的模型再审一票。另查到一条连带损失：
**lc/la 组落库要求恰好 2 题，全库有 30 个组是「一题过一题不过」→ 整组作废，白丢 30 道已经过了盲审的题。**
估计能救 ~60 题直接 + ~25 题连带。

**③ 4 套卷的 blocking 标记要人放行（未做，~188 题，结构化 ≈ ¥1）**：
- **2.23 / 3.4 / 4.18 的 `section_gap` 是陈旧的** —— `source-flags.json` 生成于 2026-09-07（`_source_audit.json`
  同批），当时配对 31~32/47；**现在重扫后都是 47/47**。
- **2.8 / 3.24 的 `ingest_blocker` detail 写的是「当前科目 reading」** —— 阅读那边早已条件放行，
  听力被同一条 `sections:["*"]` 顺带扣着。
- **4.18 的 `ingest_blocker` 写的是「当前科目 listening」** —— 这条是真的要小心（听力答案页本身出现题号重启块）。

## G3. 一个明确不建议做的

把阅读的「盲审一致率 ≥85% 条件放行」照搬到听力。阅读能这么判是因为分布有空档（正常卷 94~100% / 错位卷 45~54%）；
**听力一致率是连续分布**（64 套实测 25%~100%，合计 1518/1668 = 91.0%），0.85 会误伤一批正常卷
（1.28A 57%、2.1C 63%、4.15 68% 都是正常卷，低是因为 LAT/LA 模型本身难）。

真正的对齐信号是 **LCR 一致率**：LCR 是一句话刺激 + 四选一，模型几乎不会错，答案页一错位它立刻塌。
实测 49 套（样本 ≥5）里 41 套 ≥90%（31 套 100%），唯一的异常是 **2.1C 的 6/15 = 40%** —— 而 2.1C 的听力
本来就是「bank_dropped 23 + held 16」的重灾区。**但只有 1 个反例，样本不够立成判据**，记在这里等更多数据。

分题型一致率：LCR 94.6% · LC 92.3% · LA 88.0% · LAT 87.8%。

## G4. 三个口子的执行结果（6732 → 6810，听力 69.9%）

用户 2026-09-16 拍板「能做的、不违反规则的就做」。三件里做成两件、停掉一件。

### ② 听力补第二票 —— 做了（¥1.15）

`audit_answers.mjs <卷> --section=listening --second-vote`（模型 deepseek-v4-pro），45 套。
**130 道审完、放行 22 道（16.9%）**，分型极清楚：

| 题型 | 第二票放行 |
|---|---|
| lcr | 15/32（47%） |
| lc | 5/14（36%） |
| lat | **2/65（3%）** |
| la | **0/19（0%）** |

**这本身就是个结论**：LAT / LA 的不一致**不是第一票的失误** —— flash 与 pro 两个独立模型一起和答案页作对，
指向的是「讲座 / 通知的 ASR 转写丢了答题需要的内容」，而不是模型选错。所以那两类不该再靠加票去救，
要救得从转写质量入手。LCR / LC 的不一致才是模型失误，加一票就有近一半能回来。

### ③ 重算陈旧的 `section_gap` —— 做了（新脚本 `refresh_section_gap.mjs`）

`source-flags.json` 是 2026-09-06 的一次性体检产物，此后源料补齐过、解析器修过、卷重扫过，但 flag 没人重算。
实测 **2.23 体检时听力音频还是 `.baiduyun.p.downloading` 半个下载包、OCR 只出 31 块 → 记 blocking（整科不入库）；
现在源料下全、重扫后配对 47/47**。3.4 / 4.18 同理（32/47 → 47/47）。

判据不是新定的，就是 `source-flags.json` 自己 `_note` 里那条「缺口 ≤2 不记 / 3–9 warn / ≥10 blocking」，
只是拿**当前的扫描产物**重跑一遍。守紧的三条：只碰 `section_gap`、从不新增 flag、读不出就保留原状。
一个坑：满分必须取**蓝图**（阅读 50 / 听力 47 / 口语 11 / 写作 12），不能取「扫描里各 module 的 total 之和」——
后者会把「整个 module 在源料里就没有」算成缺口 0（3.8 的听力只有 M1，模块和是 32/32）。配 10 条测试。

结果：2.23 / 3.4 解封 → 各 27/27 组可落库（盲审 90.3% / 81.3%，LCR 10/10 与 7/8），¥0.63。
**4.18 仍扣着** —— 它的 `ingest_blocker` detail 写的是「当前科目 listening」，那是听力答案页自己出现了题号重启块，
和 2.8 / 3.24 那种「阅读页的重启块顺带扣了听力」不是一回事。3.8 的 `section_gap` 也正确保留（M2 真缺）。

### ④ 对话角色判定 —— **不做**，有硬证据

原想「题干已点明 the man + the woman 的 15 组，可以放宽基频差阈值」。查了实际分布就否了：

- **判成功的 26 组，基频差最小 63 Hz**（63 / 69 / 73 / 74 / 75 / 78 …）
- **判失败的 25 组，基频差最大 50 Hz**（20 / 27 / 27 / 27 / 29 / 31 / 38 … / 49 / 50 / 50）
- 50 ~ 63 Hz 之间**一个样本都没有**

现在的 50 Hz 阈值正卡在这段空档上。往下放就进了「没有任何成功先例」的区间 —— 那不是放宽判据，是猜。
真题 GT 155 条对话里也找不到同性别对话的措辞证据（零例「the first woman / two men」），
所以小基频差更可能是**基频估计失败**而不是同性别。

另查了一条免费路子：跨卷孪生（同一段对话在别的卷里判成功过，直接抄性别）—— 51 组里只有 2 组有 ≥0.8 的孪生，
不值得为 4 道题造机器。**这 72 题要回来，只能靠人听**（`data/realBank/listening/lc-speaker-overrides.json`
就是为此准备的口子，一组标一行 `first_speaker: male|female`）。

### 代价

全量重切原声后 4 条切片从「过」变「不过」（`tail_not_clean`），其中 3.27 的复述剩 3 句 < 5 句的成套下限 → 整套不收，
连带别名共 **-10 题**（基线 repeat 369 → 359，有意调低）。根因是 HEAD 那版清单里这几条是早先
`--only=repeat` 单独评估时留下的，这次全库同批评估才暴露；切片确实会带进下一句的开头，按规矩不该上线。
全库原声清单净增 32 条（1348 → 1376）。

## G5. 4.24 的 M1 认回来（6810 → 6833）

4.24 两个 module 都对不上蓝图（M1 13 LCR + 10 材料、M2 3 LCR + 5 材料），47 题全丢。拆开看是两个不同的病：

**① 材料在句子中间被暂停切开**（M1 多出来的那 2 段）：`…a key or a lightning ‖ bolt. A typical set could…`、
`…appeared on the canvas, the form ‖ suggested itself under his brush.` —— 后一截**小写开头**，
而既有的并段规则要求「前一截句末标点收尾 + 后一截大写开头」，正好把这种漏掉了。

判据加窄：前后**两截都够材料长度**（都被标成 mat）才并。这条能把它和「录音断流」分开 ——
断流是每隔 8~11s 掉一截，碎出来的是 odd / lcr 那种短片，**3.15 / 4.11 / 5.11 实测 19 处句中切口
没有一处是 mat→mat**；而整段只被暂停切一次，留下的是两个长半截（4.24 两处、4.18 一处）。
全库实测只多并了 4.24 的 2 处（4.18 那处间隔 8.6s 超过 8s 上限）。

**② 录音开录晚了，第一句从半截开始**（M1 多出来的第 13 条 LCR）：`button at the top of the screen.`
是 "Select the volume button…" 掉了前半截，不含 volume 二字 → INTRO_RE 认不出来 → 被当成一句 LCR。
判据只认「**整条录音的第一个岛**且从小写开头」—— 真题的刺激句都是完整句子，不会从半句起头。
全库只命中 4.24 和 5.10_v2（后者的 "green." 本来就被短噪声那条丢掉了）。

改完核了一遍：**已落库的卷里 module 版式变化 0 套**。4.24 M1 随后结构化 9 块（¥0.12）、盲审 6/6 = 100%，
落库 20 组可落库 18。**M2 仍扣下** —— 它是真断流：`…print this report now. Wh ‖ Maybe it's a software issue.`
中间整整缺了一句（"Wh[at's wrong?]"），并起来就是一段缺句的对话，按既定口径不许并。

## G6. 回退修复：原声闸的位置（09-17，6833 → 6847）

盘点进度时发现 **rf0808 的复述整套 7 句不见了** —— 它在口语切片之前一直在库里，二分定位到 `f320a0eb`
（把原声闸挪到去重之后那一笔）。

根因：rf0808 的复述与 **3.27 逐字相同**，卷名排序 3.27 在前，于是 3.27 先登记成「保留方」、rf0808 被当重复跳过；
紧接着 3.27 自己只切出 3 句干净原声（< 5 句成套下限）被原声闸扣下 —— **两边都没了**。

原声闸的位置两头都栽过：

| 闸的位置 | 后果 |
|---|---|
| 去重**之前**（最初版本） | 整套被闸空的重复卷连别名都记不上，那一卷槽位反而更空（09-16 实测 8 套） |
| 登记保留方**之后**（f320a0eb） | 过不了闸的卷照样占着保留方位置，挡掉后面自带干净原声的同内容卷（本条） |
| **去重之后、登记保留方之前**（现在） | 重复卷照样记别名；过不了闸的卷不占位，等同内容的卷登记为保留方时再补记成指向它的别名 |

结果：rf0808 整套 7 句回来，**3.27 也补记成 `real_repeat_327_1 → real_repeat_rf0808_1`**，两卷的复述槽位都满了。
全库对比 HEAD：只多了 rf0808 这一套，别的一条没动。测试改成钉住三条不变量（查重复在闸前 / 登记在闸后 / 闸掉的补别名）。

